let number;

function increment(number) {
    number--;
    --number;
}

function increment(number) {
    ++number;
    number++;
}

(몸무게 / (신장 * 신장))

// 가로를 사용해서 우선순위 지정하기
// if((isLogin && token) || user)
